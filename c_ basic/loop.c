#include<stdio.h>
int main()

{
	int x=1;       //initialization

	while(x<=5)   //here we use while 
	{
	printf("hello \n");  // condition //now here we want to print shashank 5 times for this either we have to write this statement 5 times or we use loop 
	++x; //flow                                                                                                                           //thus x=x+1 we add this operator to increase the value of variables so that after a particular process the condition will yield false and the program will end , i.e jab dobara condition change ho tab x ki value change ho jani chahiye
	}
return 0;
}
